# Vendored protocol core

These files are synchronized from `squazaryu/t-vgm-fw` protocol v1.2 for the
native Flipper client. Keep `protocol_ids.h`, `protocol.h`, and `protocol.c`
byte-equivalent to the upstream generated header and codec except for local
include paths.
